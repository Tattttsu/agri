<% if current_user.already_liked?(farm) %>
  <%= link_to "お気に入りを外す", farm_likes_path(farm), method: :delete, remote: true %>
<% else %>
  <%= link_to "お気に入りに追加", farm_likes_path(farm), method: :post, remote: true %>
<% end %>
